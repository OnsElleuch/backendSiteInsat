import { ServiceEnumEnum } from '../service-enum.enum';

export class Direction {
  title: string;
  responsable: string;
  poste: string;
  email: string;
  poste_code: number;
  service: ServiceEnumEnum;
}
